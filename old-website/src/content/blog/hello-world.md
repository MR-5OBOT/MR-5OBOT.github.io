---
title: "exit code 0"
date: 2026-06-29
desc: "first leak. it compiles, therefore it is."
---

This is a real post. To write another one, drop a new `.md` file in
`src/content/blog/` with a `title` and `date` up top. That's the whole workflow.

## why this exists

Because hand-writing an HTML page per post is how blogs die at post #3.
Markdown in, page out. The index lists itself.

## things you can do

- **bold**, *italic*, [links](https://mr-5obot.github.io)
- code: `git push --force` (don't)
- lists, quotes, headings — all the usual

> exit the matrix, enter the drawdown.

That's it. Delete this file once you've written something real.
