<a class="nav" href="/" aria-label="MR5OBOT home">
  <span class="logo-name" aria-hidden="true">
    <span>MR5OBOT</span>
  </span>
</a>

<style>
  .nav {
    position: fixed;
    top: 32px;
    top: max(32px, env(safe-area-inset-top));
    left: 50%;
    z-index: 50;
    color: #fff;
    text-decoration: none;
    transform: translateX(-50%);
  }
  .nav:hover { color: #a280ea; }
  .logo-name {
    display: flex;
    align-items: center;
    font: 400 clamp(52px, 6vw, 84px) 'VT323', monospace;
    text-shadow: 2px 2px 0 #3a1f72;
    white-space: nowrap;
  }
  .logo-name > span {
    display: block;
    width: 0;
    overflow: hidden;
    animation: type 4s steps(7, end) infinite;
  }
  .logo-name::after {
    content: '';
    width: .12em;
    height: .8em;
    margin-left: .08em;
    background: #a280ea;
    animation: blink .7s step-end infinite;
  }
  @keyframes type {
    0%, 10% { width: 0; }
    40%, 70% { width: 7ch; }
    100% { width: 0; }
  }
  @keyframes blink { 50% { opacity: 0; } }
  @media (prefers-reduced-motion: reduce) {
    .logo-name > span { width: 7ch; animation: none; }
    .logo-name::after { display: none; }
  }
  @media (max-width: 560px) {
    .nav { top: 20px; top: max(20px, env(safe-area-inset-top)); }
    .logo-name { font-size: 46px; }
  }
  @media (max-height: 500px) {
    .nav { top: 8px; top: max(8px, env(safe-area-inset-top)); }
    .logo-name { font-size: 40px; }
  }
</style>
