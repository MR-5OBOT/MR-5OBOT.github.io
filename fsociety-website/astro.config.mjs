import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // For GitHub Pages later, set:
  //   site: 'https://mr-5obot.github.io',
  //   base: '/fsociety-website',
});
